{
  "questSequence": [
    {
      "questId": "TEMP_1769968764702",
      "title": "Pull Request and Code Review",
      "description": "",
      "isQ0": false,
      "questType": "custom",
      "sequenceNumber": 4,
      "metadata": {
        "title": "Pull Request and Code Review",
        "description": "",
        "prerequisite": null,
        "type": "custom"
      },
      "badgeDescription": "Custom Quest 🎯",
      "tasks": {
        "T1": {
          "type": "iterative-code-review",
          "title": "Implement Conversion Functions ",
          "taskTitle": "Implement Conversion Functions ",
          "desc": "Implement Conversion Functions ",
          "points": 1,
          "xp": 1,
          "accept": "Implement two functions to parse a number string into its characteristic and mantissa parts.\n\n**Requirements:**\n\nThe two functions, `characteristic()` and `mantissa()`, will break a number string into a characteristic and a mantissa.\n\n- The characteristic for the number 2.351 is 2 and the mantissa is 351 over 1,000.\n- The characteristic for the number 0.0125 is 0 and the mantissa is 125 over 10,000.\n- The characteristic for the number -4.0 is -4 and the mantissa is 0 over 10.\n\nThese values should be returned by the functions.\n\n**Function Signatures:**\n```python\ndef characteristic(num_string):\n    \"\"\"\n    Extracts the characteristic (integer part) from a number string.\n    \n    Returns:\n        tuple: (bool, int) - (True, characteristic) if valid, (False, 0) if invalid\n    \"\"\"\n    pass\n\ndef mantissa(num_string):\n    \"\"\"\n    Extracts the mantissa (fractional part) from a number string.\n    \n    Returns:\n        tuple: (bool, int, int) - (True, numerator, denominator) if valid, (False, 0, 0) if invalid\n    \"\"\"\n    pass\n```\n\n**How to Submit:**\n1. Create a new branch in your repository:\n   ```bash\n   git checkout -b conversion-functions\n   ```\n\n2. Create a file called `conversion.py` with your implementation\n\n3. Add and commit your code:\n   ```bash\n   git add conversion.py\n   git commit -m \"Add characteristic and mantissa functions\"\n   ```\n\n4. Push your branch to GitHub:\n   ```bash\n   git push origin conversion-functions\n   ```\n\n5. Create a Pull Request from your branch to `main`:\n   - Go to your repository on GitHub\n   - Click \"Pull requests\" → \"New pull request\"\n   - Select your branch (`conversion-functions`) → base branch (`main`)\n   - Click \"Create pull request\"\n   - Copy the PR link (e.g., `https://github.com/YourOrg/YourRepo/pull/2`)\n\n6. Post the PR link in a comment on this issue, or just type the PR number (e.g., `#2`)\n\n**The bot will review your code and provide feedback. Fix any issues and push new commits to the same PR. The bot will automatically review the updated code!**\n\n### Code Language\n```\npython\n```",
          "success": "### 🌟 Congratulations! You Nailed It!",
          "error": "### 🚨 Oops, That's Not Quite Right!",
          "answer": "",
          "correctAnswer": "",
          "question": "",
          "options": [
            {
              "label": "A",
              "value": ""
            },
            {
              "label": "B",
              "value": ""
            }
          ],
          "repository": "",
          "ossRepository": "",
          "issueNumber": "",
          "apiEndpoint": "",
          "responsePath": "",
          "expectedAnswerType": "Number",
          "enableTolerance": false,
          "toleranceRange": 10,
          "saveValidatedData": false,
          "savedDataName": "",
          "questions": [],
          "hints": [],
          "detailedHints": [],
          "llmTextValidation": {
            "question": "",
            "validationParameters": [],
            "temperature": 0.1,
            "enableDetailedFeedback": false
          },
          "imageValidation": {
            "question": "",
            "validationParameters": [],
            "temperature": 0.1,
            "enableDetailedFeedback": false
          },
          "questNotes": "",
          "answerType": "",
          "repositoryToBeStored": "",
          "requireOwnership": true,
          "allowForkOfFork": true,
          "storeForkUrl": true,
          "expectedFileName": "",
          "expectedFileType": "",
          "targetRepository": "",
          "targetBranch": "main",
          "sourceRepository": "",
          "requireOpenState": true,
          "requireRecentPush": false,
          "recentPushWindowHours": 24,
          "expectedFilePath": "",
          "fileContentValidation": {
            "question": "",
            "validationParameters": [],
            "temperature": 0.1,
            "enableDetailedFeedback": false
          },
          "iterativeCodeReview": {
            "reviewPrompt": "- Function must check for None input before processing\n- Function must handle leading and trailing whitespace\n- Function must validate all input characters (reject invalid characters)\n- Function must handle unary plus and minus signs\n- Function must correctly parse integer part (characteristic)\n- Function must correctly parse fractional part (mantissa)\n- Function must handle case with no decimal point (mantissa should return 0/10)\n- Function must return (False, 0) or (False, 0, 0) for invalid input strings\n- Function must return (True, value) or (True, numerator, denominator) for valid input strings\n- Function must skip whitespace around decimal point\n- Function must handle empty strings and strings with only signs\n- Function must not crash on index out of bounds errors",
            "reviewCriteria": [],
            "maxIterations": 3,
            "temperature": 0.3,
            "enableDetailedFeedback": true,
            "prFilePattern": "",
            "commentOnPR": true,
            "approvePR": true,
            "mergePR": true,
            "mergeMethod": "merge"
          },
          "botCodeReview": {
            "acceptMessage": "",
            "originalCode": "",
            "codeLanguage": "cpp",
            "requirements": "",
            "knownIssues": [],
            "maxIterations": 5,
            "temperature": 0.3,
            "enableDetailedFeedback": true,
            "awardPointsOnFailure": false,
            "createPR": false,
            "validateOnApproval": true,
            "fileName": "code.cpp"
          }
        },
        "T2": {
          "title": "Math Functions Code Review",
          "taskTitle": "Math Functions Code Review",
          "desc": "Math Functions Code Review",
          "points": 1,
          "xp": 1,
          "hints": [],
          "detailedHints": [],
          "type": "bot-code-review",
          "botCodeReview": {
            "acceptMessage": "",
            "originalCode": "def add(c1, n1, d1, c2, n2, d2, result, max_len):\n    \"\"\"\n    Adds two numbers represented as characteristic and mantissa.\n    \n    Args:\n        c1, n1, d1: First number as (c1 + n1/d1)\n        c2, n2, d2: Second number as (c2 + n2/d2)\n        result: List to store the result string\n        max_len: Maximum length of result string\n    \n    Returns:\n        bool: True if characteristic fits, False otherwise\n    \"\"\"\n    # Convert to decimals\n    num1 = c1 + (n1 / d1)\n    num2 = c2 + (n2 / d2)\n    total = num1 + num2\n    \n    # Convert to string\n    result_str = str(total)\n    \n    # Check if characteristic fits\n    if len(result_str) > max_len:\n        return False\n    \n    # Copy to result\n    for i in range(len(result_str)):\n        result[i] = result_str[i]\n    \n    return True\n\n\ndef subtract(c1, n1, d1, c2, n2, d2, result, max_len):\n    \"\"\"\n    Subtracts two numbers represented as characteristic and mantissa.\n    \n    Args:\n        c1, n1, d1: First number as (c1 + n1/d1)\n        c2, n2, d2: Second number as (c2 + n2/d2)\n        result: List to store the result string\n        max_len: Maximum length of result string\n    \n    Returns:\n        bool: True if characteristic fits, False otherwise\n    \"\"\"\n    # Convert to decimals\n    num1 = c1 + (n1 / d1)\n    num2 = c2 + (n2 / d2)\n    diff = num1 - num2\n    \n    # Convert to string\n    result_str = str(diff)\n    \n    # Check if characteristic fits\n    if len(result_str) > max_len:\n        return False\n    \n    # Copy to result\n    for i in range(len(result_str)):\n        result[i] = result_str[i]\n    \n    return True",
            "codeLanguage": "python",
            "requirements": "- Functions must return `True` if at minimum the characteristic of the result can be stored\n- If the result is non-integer, place as many digits as will fit after the decimal point\n- Functions must return `False` if the result cannot fit (including null terminator)\n- The result array must end with a null terminator ('\\0')\n- You cannot use floating-point arithmetic - you must work with integers and fractions\n- Functions must validate inputs (check for division by zero, null result array, etc.)",
            "knownIssues": [
              "Using floating-point arithmetic instead of integer/fraction arithmetic - should work with fractions directly",
              "Missing null terminator ('\\0') at end of result array",
              "Missing input validation - should check for division by zero (d1 == 0 or d2 == 0)",
              "Missing input validation - should check if result is None",
              "Missing input validation - should check if max_len is valid (at least 1)",
              "Not checking if characteristic fits correctly - should account for null terminator space",
              "Not handling case where result doesn't fit - should return False if characteristic + null terminator exceeds max_len",
              "Not placing as many decimal digits as will fit - should continue adding digits until max_len is reached",
              "Not working with fractions properly - should use common denominator approach instead of floating-point"
            ],
            "maxIterations": 3,
            "temperature": 0.3,
            "enableDetailedFeedback": true,
            "awardPointsOnFailure": false,
            "createPR": true,
            "validateOnApproval": true,
            "fileName": "math_functions.py"
          },
          "accept": "I'm trying to implement two functions to add and subtract numbers represented as characteristic and mantissa, but I think there might be something wrong with my code.\n\n**What I'm trying to do:**\n\nThese two functions take as parameters two sets of mantissa and characteristic and a result array to hold the result of the arithmetic operations. The result of the add or subtract should be converted into characters and placed in the result array. The array must end with a null terminator ('\\0'). The 'max_len' parameter tells how many characters can be placed in the result array.\n\nPlease let me when you are ready to go",
          "success": "### 🌟 Congratulations! You Nailed It!",
          "error": "### 🚨 Oops, That's Not Quite Right!",
          "answer": ""
        }
      }
    }
  ]
}